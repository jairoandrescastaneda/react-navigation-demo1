import { StyleSheet } from 'react-native';

export const main = StyleSheet.create({
  panCenter: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  txtSection: {
    textAlign: 'center', 
    fontWeight: 'bold', 
    marginBottom: 10,
  }
});
